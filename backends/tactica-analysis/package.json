{
  "name": "@tactica/tactica-analysis",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist .turbo",
    "db:clean": "pnpm db:up && ../../tools/scripts/drop_db.sh tactica_analysis tactica_postgres_dev && ../../tools/scripts/drop_user.sh tactica_analysis tactica_postgres_dev",
    "db:clean:test": "pnpm db:up:test && ../../tools/scripts/drop_db.sh tactica_analysis tactica_postgres_test && ../../tools/scripts/drop_user.sh tactica_analysis tactica_postgres_test",
    "db:dump-fixtures": "../../tools/scripts/dump_fixtures.sh tactica_analysis",
    "db:generate-custom-migration": "pnpm --filter @tactica/drizzle-utils build && ../../tools/scripts/generate_custom_db_migration.sh",
    "db:generate-migration": "pnpm --filter @tactica/drizzle-utils build && pnpm db:up && ../../tools/scripts/generate_db_migration.sh",
    "db:migrate-up": "pnpm db:up && tsx --conditions=development ./src/db/migrate.ts",
    "db:migrate-up:test": "pnpm db:up:test && ANALYSIS_DATABASE_URL=\"postgres://tactica_analysis:tactica_analysis_password@localhost:5441/tactica_analysis?sslmode=disable\" tsx --conditions=development ./src/db/migrate.ts",
    "db:restore-fixtures": "pnpm db:clean && pnpm db:up && ../../tools/scripts/restore_fixtures.sh tactica_analysis && pnpm db:migrate-up",
    "db:up": "../../tools/scripts/ensure_postgres_db.sh -d tactica_analysis -c tactica_postgres_dev && ../../tools/scripts/ensure_postgres_user.sh -u tactica_analysis -w tactica_analysis_password -d tactica_analysis -c tactica_postgres_dev",
    "db:up:test": "../../tools/scripts/ensure_postgres_db.sh -d tactica_analysis -c tactica_postgres_test && ../../tools/scripts/ensure_postgres_user.sh -u tactica_analysis -w tactica_analysis_password -d tactica_analysis -c tactica_postgres_test",
    "format": "eslint \"src/**/*.ts\" --fix",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts\"",
    "serve": "tsx watch --conditions=development src/main.ts",
    "start": "node dist/main.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@tactica/drizzle-utils": "workspace:*",
    "@tactica/errors": "workspace:*",
    "@tactica/fastify-utils": "workspace:*",
    "@tactica/logging": "workspace:*",
    "@tactica/stockfish-contract": "workspace:*",
    "@tactica/tactica-analysis-contract": "workspace:*",
    "@ts-rest/core": "^3.52.1",
    "@ts-rest/fastify": "^3.52.1",
    "chess.js": "^1.4.0",
    "drizzle-orm": "^0.44.5",
    "fastify": "^4.28.1",
    "pg-boss": "^12.26.3",
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@tactica/eslint-config": "workspace:*",
    "@tactica/tsconfig": "workspace:*",
    "@types/node": "^22.10.0",
    "@types/supertest": "^6.0.2",
    "drizzle-kit": "^0.31.4",
    "eslint": "^9.34.0",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.8"
  }
}
